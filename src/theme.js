import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#7C74FC',
    },
    secondary: {
      main: '#242424',
    },
    default:{
      main: '#474492'
    }
  },
});

export default theme;